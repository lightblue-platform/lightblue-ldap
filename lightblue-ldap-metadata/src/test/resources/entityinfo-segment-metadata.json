{
    "entityInfo": {
        "name": "fakeEntity",
        "datastore": {
            "backend":"ldap",
            "database": "${ldap.database}",
            "basedn": "${ldap.basedn}",
            "uniqueattr": "${ldap.uniqueattr}"
        }
    }
}
